import type { BracketMatch } from '@/engine/types'

interface KnockoutMatchModalProps {
  match: BracketMatch
  roundLabel: string
  onClose: () => void
}

export function KnockoutMatchModal(_props: KnockoutMatchModalProps) {
  return null
}
